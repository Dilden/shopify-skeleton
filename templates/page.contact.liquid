
<div class="flex flex-col md:flex-row justify-center gap-8 mx-auto max-wd-[90vw] md:max-w-[80vw]">
    {%- if page.content != blank and page.title != blank} -%}
      <div class="basis-1/2 mx-4">
        <h2 class="text-3xl">{{ page.title }}</h2>
        {{ page.content }}
      </div>
    {%- endif -%}
    <div class="basis-1/2 text-left mx-4">
    {%- section 'contact-form' -%}
    </div>
</div>
